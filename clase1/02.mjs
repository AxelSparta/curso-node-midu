// ES Modules, mejor forma, más moderna y la recomendada para importar/exportar modulos...
// en ES Modules la extensión al importar archivos es obligatoria
import { sum } from './sum.mjs'

console.log(sum(1, 2))